name: Hadolint Linting
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  tflint:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest]

    steps:
      - name: Hadolint Action
        uses: hadolint/hadolint-action@v3.0.0
        with:
          config: .hadolint.yaml